@model IEnumerable<WebApp.DTO.ParkingLotDTO>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<h1>Parking Lot List</h1>
<form class="search1">
    <div class="search__wrapper">
        @Html.TextBox("SearchString", "", new { @class = "search__field" })
        <button type="submit" class="fa fa-search search__icon"></button>
    </div>
</form>
@using (Html.BeginForm()) {
    @*<div class="input-group" id="searchbar">
            @Html.TextBox("SearchString", "", new { @class = "form-control rounded" })
            <button type="submit" class="btn btn-outline-primary" value="Search" placeholder="Search...">search</button>
        </div>*@



}



<div class="page-content">
    @foreach (var item in Model) {
        @*<div class="parking-lot-reservation-card">
                <p>Parking Lot Name: @Html.DisplayFor(modelItem => item.name)</p>
                <p>Location: @Html.DisplayFor(modelItem => item.location)</p>
                <div class="parking-lot-reservation-card-buttons">
                    <p>@Html.ActionLink("Now", "Now", new { id = item.parkingLotID })</p>
                    <p>@Html.ActionLink("Later", "Later", new { id = item.parkingLotID })</p>
                </div>
            </div>*@




        <div class="card-content">
            <img src="@Html.DisplayFor(modelItem => item.imageURL)" class="card-img-top" alt="...">
            <div class="card-body-content">
                <h2 class="centered">@Html.DisplayFor(modelItem => item.name)</h2>
                <p class="centered2">@Html.DisplayFor(modelItem => item.location)</p>
                <div id="buttons" class="card-body-buttons">
                    <div class="parking-lot-reservation-card-buttons"><a id="btn-now" class="reservation-about-btn" asp-area="" asp-controller="MakeReservation" asp-action="Now" asp-route-id="@item.parkingLotID">BOOK NOW</a></div>
                    <div class="parking-lot-reservation-card-buttons"><a id="btn-later" class="reservation-about-btn" asp-area="" asp-controller="MakeReservation" asp-action="Later" asp-route-id="@item.parkingLotID">BOOK FOR LATER</a></div>
                </div>
            </div>
        </div>
    }
</div>
